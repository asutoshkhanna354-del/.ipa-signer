#!/bin/bash
  # Wrapper: adds -destination for archive builds (Xcode 16.2+)
  REAL="/Applications/Xcode_16.2.app/Contents/Developer/usr/bin/xcodebuild"
  if [[ " $* " == *" archive "* ]]; then
    exec "$REAL" "$@" -destination 'generic/platform=iOS'
  fi
  exec "$REAL" "$@"
  